/*
In this little assignment you are given a string of space separated numbers, and have to return the highest and lowest numb
*/

function highAndLow(numbers){
  let arr = numbers.split(' ')
let lowest = Math.min(...arr)
let highest = Math.max(...arr)
return  `${highest} ${lowest}`
  }
