/*
Make a simple function called greet that returns the most-famous "hello world!".
*/

// Write a function "greet" that returns "hello world
function greet(robot) {
  let robotHi = 'hello world!'
  return robotHi;
}
