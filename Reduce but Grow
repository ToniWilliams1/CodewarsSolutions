/* Given a non-empty array of integers, return the result of multiplying the values together in order. 
*/

function grow(x){
let result = x.reduce((previousValue, currentValue)=> previousValue * currentValue,1)
  return result
  }
